<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/computer">
            <i class="bi bi-pc-display me-2"></i> <strong>Computer Center</strong>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <% if (typeof user !== 'undefined' && user) { %>

                    <li class="nav-item">
                        <a class="nav-link active" href="/computer">Головна</a>
                    </li>

                    <% if (user.role === 'DB_ADMIN') { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                Управління
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/session">Сесії</a></li>
                                <li><a class="dropdown-item" href="/user">Користувачі</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/reports">Звіти</a></li>
                            </ul>
                        </li>
                    <% } %>

                    <% if (user.role === 'OPERATOR') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/monitor">Моніторинг</a>
                        </li>
                    <% } %>

                <% } %>
            </ul>

            <ul class="navbar-nav ms-auto">
                <% if (typeof user !== 'undefined' && user) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                            <span class="me-2"><%= user.login %></span>
                            <span class="badge bg-secondary"><%= user.role %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/profile">Мій профіль</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="/auth/logout">Вийти</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">Вхід</a>
                    </li>
                <% } %>
            </ul>

        </div>
    </div>
</nav>