<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/computer">
            <i class="bi bi-pc-display me-2"></i> <strong>Computer Center</strong>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <% if (typeof user !== 'undefined' && user) { %>

                    <li class="nav-item">
                        <a class="nav-link active" href="/computer">Головна</a>
                    </li>

                    <% if (user.role === 'DB_ADMIN') { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                Управління
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/session">Сесії</a></li>
                                <li><a class="dropdown-item" href="/user">Користувачі</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/report">Звіт</a></li>
                            </ul>
                        </li>
                    <% } %>

                    <% if (user.role === 'OPERATOR') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/monitor">Моніторинг</a>
                        </li>
                    <% } %>

                <% } %>
            </ul>

            <div class="navbar-nav ms-auto d-flex align-items-center gap-3">
                <% if (typeof user !== 'undefined' && user) { %>
                    
                    <div class="text-end lh-1 d-none d-lg-block">
                        <div class="fw-bold text-light"><%= user.login %></div>
                        <small class="text-white-50" style="font-size: 0.75rem;"><%= user.role %></small>
                    </div>
            
                    <div class="vr text-white opacity-25 d-none d-lg-block" style="height: 25px;"></div>
            
                    <a href="/auth/logout" class="btn btn-outline-light btn-sm d-flex align-items-center gap-2" title="Вийти з системи">
                        <i class="bi bi-box-arrow-right"></i>
                        <span class="d-lg-none">Вийти</span> </a>
            
                <% } else { %>
                    <a href="/auth/login" class="btn btn-primary btn-sm px-4 fw-bold">Вхід</a>
                <% } %>
            </div>

        </div>
    </div>
</nav>